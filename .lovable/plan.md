Plano:

1. Mover o botão **Postar** para o cabeçalho da obra, ao lado/abaixo do título, em uma posição imediatamente visível no topo da página.

2. Manter a regra atual: o botão aparece apenas quando `isAdmin === true`; visitantes continuam sem ver a ação.

3. Remover o botão duplicado da área abaixo do player de audiodescrição para evitar confusão.

4. Preservar o link atual para `/postar/$num`, apontando para a página já criada de geração do reels.

5. Verificar no navegador que `/obras/2` renderiza o botão com a sessão de admin.